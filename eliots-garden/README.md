## Eliot's Garden — The Waste Land

Run the app:

```bash
npm run dev
```

The app loads `public/wasteland.txt`, parses into lines and space tokens, and renders every token as an annotatable unit. Left panel shows animated annotations; right panel holds an arc diagram and explorer stub. Styling is minimalist, white on black, using the `Literata` serif family.
